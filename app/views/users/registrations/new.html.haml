.d-flex.justify-content-center.align-items-center.flex-column.vh-100
  %h2.text-primary Sign up
  = form_with(model: resource, as: resource_name, url: user_registration_path, data: { turbo: false }) do |f|
    .mb-3
      = f.label :name, class: 'mb-1' do
        %span User name
        %abbr.text-danger *
      = f.text_field :name, autofocus: true, autocomplete: 'off', class: 'form-control', placeholder: 'Username', required: true

    .mb-3
      = f.label :email, class: 'mb-1' do
        %span Email Address
        %abbr.text-danger *
      = f.email_field :email, autofocus: true, autocomplete: 'off', class: 'form-control', placeholder: 'email@example.com', required: true

    .mb-3
      .mb-1.d-flex.justify-content-between
        = f.label :password do
          %span Password
          %abbr.text-danger *
      = f.password_field :password, class: 'form-control', required: true, autocomplete: 'off', placeholder: 'Password'

    .mb-3
      .mb-1.d-flex.justify-content-between
        = f.label :password_confirmation do
          %span Password Confirmation
          %abbr.text-danger *
      = f.password_field :password_confirmation, class: 'form-control', required: true, autocomplete: 'off', placeholder: 'Password Confirmation'

    .mb-3
      = f.submit "Sign up", class: 'btn btn-primary w-100'

    .mb-3
      .text-center.link-to-sign-in
        %i Already have an account?
        = link_to 'Sign In', new_session_path(resource_name)

    .mt-5.d-flex.small
      %strong Note:
      .px-1.text-black-50
        Password contain at least #{@minimum_password_length} simple characters